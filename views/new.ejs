<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
    />
    <title>new SaifURL</title>
  </head>
  <body>
    <form action="/shortUrls" method="POST" class="my-4">
      <div class="row">
        <!-- Full URL -->
        <div class="col-md-4">
          <label for="fullUrl" class="form-label">Full URL</label>
          <input
            required
            placeholder="Url"
            type="url"
            name="fullUrl"
            id="fullUrl"
            class="form-control"
          />
        </div>

        <!-- Access Token -->
        <div class="col-md-3">
          <label for="token" hidden class="form-label">Access Token</label>
          <input
            required
            type="hidden"
            name="token"
            id="token"
            value="<%= token %>"
            placeholder="Enter Access Token"
            class="form-control"
          />
        </div>

        <!-- Custom Short URL -->
        <div class="col-md-3">
          <label for="customShortUrl" class="form-label"
            >Custom Short URL</label
          >
          <input
            type="text"
            name="customShortUrl"
            id="customShortUrl"
            placeholder="Custom short URL"
            class="form-control"
          />
        </div>

        <!-- Submit Button -->
        <div class="col-md-2">
          <button class="btn btn-success mt-4" type="submit">Short</button>
        </div>
      </div>
    </form>
  </body>
</html>
